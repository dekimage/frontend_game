import { Children, useEffect } from "react";




const newAuthContext = export const Context = createContext(i);
const RandomContext = export const Context = createContext(i);

const useAuthContext = () => {
  return newAuthContext
};

const up

const useRandomContext = () => {
  return RandomContext
};



const AuthProveder = () => {
  const [Cards, setCards] = useState()

  const CardHandler = (data) => {
    setCards(data)
  }
  
  useEffect() => {
    //make call
    cardHandler(data)
    const token = Token.get()
    if(Token){
      //make call
      loginUser(dispath, {})
    }
  },[]
}

const RandomProvider = () => {
  return (

  )
}


return (
  <AuthProveder>
  <updateAuthProver>
    <RandomProvider>
      {Children}
    </RandomProvider>
    <updateAuthProver>
  </AuthProveder>
)
