UPDATE USER RELATION FIELD

 const data = await strapi.plugins["users-permissions"].services.user.edit(
      { id: user.id },
      {
        followers: [...user.followers, buddyId],
      }
    );

UPDATE USERCARDS

UPDATE 